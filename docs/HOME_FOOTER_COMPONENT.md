# 2013 Home Footer Component

The footer component follows the raw 2013 Home capture under `references/Home/`.
It keeps the captured link grouping, language trigger, genre links, legal block,
classes, and content hierarchy. Snapshot-specific advertising content and the
external Truste embed were deliberately excluded; neither is a required footer
structure nor a safe local dependency.
